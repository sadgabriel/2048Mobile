# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 프로젝트 개요

Unity로 만드는 모바일 퍼즐 게임 프로젝트. 오리지널 아이디어는 다음 작품을 위해 남겨두고, 이번
프로젝트는 학습을 목적으로 이미 유명한 게임인 **2048**을 클론 형태로 만든다. 2048은 4x4 그리드에서
같은 숫자 타일끼리 밀어 합치는 퍼즐 게임으로, 스와이프/방향키 입력, 타일 합치기 로직, 매 턴 랜덤
타일 생성, 승리(2048 타일 생성)/패배(더 이상 움직일 칸 없음) 판정 등이 핵심 매커닉이다.

## 개발 워크플로우

이 프로젝트는 Claude Code를 적극 활용해 개발한다 — 기능 설계, 코드 작성, 리팩토링, 디버깅 등
개발 전반에서 협업 파트너로 사용할 계획. 구체적인 워크플로우 규칙(브랜치 전략, 커밋 컨벤션 등)은
아직 정해지지 않았으며, 필요해지는 시점에 이 문서에 추가한다.

## 현재 상태

저장소는 아직 비어 있음 (커밋 없음, 소스 없음). 빌드 시스템, 테스트, 코드 아키텍처는 존재하지 않음.

Unity 프로젝트 구조가 생기면 이 문서를 갱신해서 다음을 포함할 것:
- 빌드, 린트, 테스트 실행 명령 (단일 테스트 실행 방법 포함)
- 여러 파일을 함께 봐야 이해할 수 있는 상위 수준 아키텍처
